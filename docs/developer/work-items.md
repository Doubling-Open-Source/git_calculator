# Work items

Load scope and delivery conventions from **Linear** (`DOU-*` on the Doubling team), especially issues labeled `git-calculator-python`.

Treat the issue’s acceptance criteria as the session boundary. Record user-visible product changes in a Changeset (`docs/changesets.md`). Durable product behavior belongs in MDCP shards under `docs/features/` and `docs/client/`, not in Linear comments.

Planning and tickets stay in Linear. Do not put backlogs in durable shards.
