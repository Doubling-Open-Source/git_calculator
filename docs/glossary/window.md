# window

The UTC time range that decides which commits and cycle-time samples count: `--from` and `--to`, half-open `[from, to)`. Distinct from [grain](grain.md), which only decides how counted commits become rows. Contract: [window and grain](../features/window-and-grain.md). Usage: [choose window and grain](../client/choose-window-and-grain.md).
