# commit summary

Git’s subject line (`%s`): the first line of the message. In squash-merge workflows this is typically the pull-request title. Distinct from the **body** (`%b`), which after a squash often concatenates earlier commit messages and can mention “fix” without describing the landed change.
